<h1>Bundles Cadastrados</h1>

<%if @product_bundles.empty? %>
  Não existem bundles cadastrados no sistema
<% else %>
  <% @product_bundles.each do |p| %>
    <h3><%=link_to(p.name, p) %></h3>
    SKU: <%= p.sku_code %>
  <% end %>
<% end %>

<br><%= link_to 'Voltar', root_path %>