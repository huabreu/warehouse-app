<h1>Novo Lote de Produtos</h1>

<%= form_with(url: product_items_entry_path) do |form| %>
  <div>
    <%= form.label :product_model_id, 'Produto' %>
    <%= form.collection_select :product_model_id, @product_models, :id, :name, {include_blank: true} %>
  </div>
  <div>
    <%= form.label :warehouse_id, 'Galpão Destino' %>
    <%= form.collection_select :warehouse_id, @warehouses, :id, :code, {include_blank: true} %>
  </div>
  <div>
    <%= form.label :quantity, 'Quantidade' %>
    <%= form.number_field :quantity %>
  </div>

  <div>
    <%= form.submit 'Confirmar' %>
  </div>
<% end %>